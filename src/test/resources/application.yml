api:
  endpoint:
    base-url: /api/v1

schedules:
  cron:
    user:
      delete: 0/2 * * * * *
    post:
      delete: 0/2 * * * * *

images:
  dir:
    user-profile: path

slack:
  token: token

spring:
  jpa:
    show-sql: true
